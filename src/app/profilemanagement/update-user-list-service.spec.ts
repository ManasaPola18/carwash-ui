import { UpdateUserListService } from './update-user-list-service';

describe('UpdateUserListService', () => {
  it('should create an instance', () => {
    expect(new UpdateUserListService()).toBeTruthy();
  });
});
